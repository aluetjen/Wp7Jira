<phone:PhoneApplicationPage 
    x:Class="Aluetjen.Jira.Contexts.Import.Mvvm.SignInPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <StackPanel>
            <TextBlock Text="JIRA Login" Style="{StaticResource PhoneTextExtraLargeStyle}" />
            
            <TextBlock Text="URL:" />
            <TextBox x:Name="JiraUrl" />
            <TextBlock Text="User:"/>
            <TextBox x:Name="UserName" />
            <TextBlock Text="Password:"/>
            <PasswordBox x:Name="Password"/>
            <Button x:Name="SignIn" Click="SignIn_Click">Sign in</Button>
        </StackPanel>
    </Grid>
 
</phone:PhoneApplicationPage>
